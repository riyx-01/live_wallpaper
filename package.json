{
  "name": "whisperwall-root",
  "version": "1.0.0",
  "type": "module",
  "description": "Shared live wallpaper experience for couples and families",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run install-all && npm run build --prefix client",
    "server": "node server/server.js",
    "client": "node client/node_modules/vite/bin/vite.js --root client",
    "dev": "node dev.js",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
